__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
logs/
# Intermediate training checkpoints — re-creatable by running train.py.
checkpoints/hermes_a_iter*.pt
checkpoints/hermes_b_iter*.pt
# Reparameterized deploy checkpoints are tracked — they are the deliverable.
# Local visual outputs of scripts/upscale.py — not tracked.
demo/
.DS_Store

# Distillation teacher weights (large, download separately)
teacher/
# WIP MATLAB-imresize port (unverified)
hermes_sr/data/imresize.py
